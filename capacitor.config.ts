import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.alejandrorodm.ticketsaver',
  appName: 'TicketSaver',
  webDir: 'dist'
};

export default config;
